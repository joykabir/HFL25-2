## HeroDex 3000 command-line application with an entrypoint in `bin/`, library code
in `lib/`, and example unit test in `test/`.

### Prerequisites
- Dart SDK 3.8 or higher
- Git

### Run
```bash
cd v02
dart pub get
dart run
dart test
```